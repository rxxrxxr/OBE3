<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ReviewMapper">
    <insert id="save"
            parameterType="ReviewVo">
        insert into review
        (review_id, member_id, store_id, star, review, reply, reviewDelete_yn, replyDelete_yn)
        values
        (#{review_id}, #{member_id}, #{store_id}, #{star}, #{review}, #{reply}, #{n}, #{n})
    </insert>
</mapper>